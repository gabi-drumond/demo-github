# Copia este archivo como .env (NO lo subas al repo).
# En la demo de secret scanning / push protection, muestra que una cadena como esta
# NUNCA debe ir en el codigo; GitHub la bloquea al hacer push.
DATABASE_URL=postgres://usuario:CAMBIA_ESTA_CLAVE@localhost:5432/contoso
PORT=3000
